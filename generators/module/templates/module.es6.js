export default class <%= className %> {
    constructor() {
        this.name = '<%= name %>';
        console.log('%s module', this.name.toLowerCase());

        this.init();
    }

    init() {
        
    }
}
